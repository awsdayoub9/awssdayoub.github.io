---
interface Props { cols?: '2' | '3' | 'auto' | 'creds'; class?: string }
const { cols = 'auto', class: cls } = Astro.props;
---
<div class:list={['cellgrid', `cellgrid--${cols}`, cls]}><slot /></div>
